# MSA 기반 온라인 강의 플랫폼 이해 보고서
> ㅇ반 ㅇㅇㅇ 이름

---

## Chapter 1. 고객 중심 서비스 이해

### 1. 서비스 개요

이번 과제에서는 Eureka, OAuth2/JWT 인증 서버, API Gateway, Kafka로 구성된 MSA 온라인 강의 수강 플랫폼을 다뤄봤다. 회원가입·로그인, 강의 조회·등록, 수강신청, 결제, 추천이라는 한 덩어리의 흐름을 user-service, course-service, enrollment-service, payment-service, recommend-service 다섯 개 서비스로 쪼개놓은 구조인데, 처음에는 "이걸 굳이 왜 나눠놨을까" 싶었지만 직접 기동해보고 나서야 서비스 디스커버리와 인증 위임, 이벤트 기반 통신이 실제로 어떻게 맞물리는지 감이 왔다. 상용 서비스 수준의 완성도를 목표로 한 게 아니라, MSA의 핵심 구조를 손으로 눌러보고 눈으로 확인하는 데 초점이 맞춰진 교육용 레퍼런스다.

### 2. 학습자에게 제공하는 가치

가장 크게 체감한 건 의존성 순서대로 서비스가 기동되는 과정이다. `docker compose up` 한 번으로 MariaDB/Kafka부터 시작해 eureka-server, auth-server, api-gateway, 도메인 서비스, recommend-service 순으로 올라오는데, Eureka 대시보드(`localhost:8761`)를 계속 새로고침하면서 서비스가 하나씩 UP으로 바뀌는 걸 보는 재미가 있었다.

인증 위임 구조도 인상적이었다. 클라이언트가 매 요청마다 JWT를 직접 검증하는 게 아니라, API Gateway가 검증을 끝내고 `X-User-Id`만 하위 서비스로 넘겨준다. 로그인 흐름을 따라가 보면 이 부분이 자연스럽게 이해된다.

그 외에 수강신청→결제가 하나의 동기 트랜잭션이 아니라 Kafka 이벤트로 이어지는 비동기 사가(Saga)라는 점, 별도 클라우드 인프라 없이 로컬 Docker Compose만으로 전체 스택을 재현할 수 있다는 점도 실습 환경으로서 장점이었다.

### 3. 주요 고객 적용 사례

수강생: 로그인 후 강의 탐색, 신청·결제, 마이페이지에서 추천 강의 확인
강사: 로그인 후 신규 강좌 등록, 본인 강좌의 수강 현황 파악
실습 진행자(교수자·운영자): 전체 스택 기동 후 Eureka 대시보드에서 서비스 등록 상태 점검 — 실습 중에는 이 역할을 가장 많이 반복했다

### 4. 고객이 느낄 수 있는 한계

서비스별 DB가 분리되어 있지 않음 → user/course/enrollment/payment-service가 물리적으로 하나의 MariaDB(`lecture_db`)를 공유해, 서비스마다 자신의 데이터를 소유한다는 MSA 원칙과 어긋남
서비스 간 호출이 Eureka `lb://`가 아닌 컨테이너 DNS로 고정되어 있음 → 디스커버리는 등록만 담당할 뿐 실제 호출 경로 결정에는 관여하지 않음
결제가 실제 PG 연동 없는 시뮬레이션임 → 결제 실패, 재시도, 보상 트랜잭션 같은 장애 상황은 다루지 않음
Kafka 이벤트에 타입 정보가 없어 `Map<String,Object>`로만 파싱됨 → 필드가 바뀌어도 컴파일 타임에 걸러지지 않음
서비스 간 동기 호출에 circuit breaker·retry 같은 회복탄력성 패턴이 없음

### 5. 구조 전환 제안

위 다섯 가지를 전부 고칠 자신은 없어서, 시간 안에 손댈 수 있을 만한 것과 이 프로젝트 목적상 굳이 안 건드려도 되겠다 싶은 것을 나눠봤다.

가장 먼저 손대고 싶은 건 서비스별 DB 분리(Database per Service)다. 지금처럼 하나의 `lecture_db`를 공유하면 스키마 하나 바꿀 때마다 다른 서비스에 영향이 없는지 계속 확인해야 하는데, 이게 MSA를 배우는 의미를 가장 크게 깎아먹는 부분이라고 생각한다.

두 번째는 하드코딩된 컨테이너 DNS를 `@LoadBalanced` + Eureka `lb://` 기반 호출로 바꾸는 것이다. 지금 구조는 Eureka에 등록은 하면서 정작 서비스를 찾을 때는 쓰지 않아서, 디스커버리가 있으나 마나 한 상태다.

circuit breaker·재시도 같은 회복탄력성 패턴이나 결제 보상 트랜잭션은 필요성은 느꼈지만 이번 과제 범위에서는 시간 관계상 손대지 못했다. Kafka 이벤트 타입 스키마 문제도 마찬가지로, Avro나 스키마 레지스트리까지 도입하는 건 이 규모의 실습 프로젝트치고는 과하다고 판단해 우선순위에서 뺐다.

---

## Chapter 2. 시스템 기술 구조 및 설계 이유

### 1. 모놀리식 → MSA 전환 배경

회원·강좌·수강·결제 로직이 하나의 애플리케이션에 뭉쳐 있으면, 배포 단위와 장애 범위가 전체 서비스로 확산된다. 결제 로직 하나를 고치기 위해 전체를 재배포해야 하고, 결제 모듈의 장애가 강좌 조회 기능까지 마비시킬 수 있다. MSA는 도메인별로 서비스를 쪼개 독립 배포·독립 장애 범위를 확보하고, 서비스 간에는 REST(동기)와 이벤트(비동기)로만 통신하게 해서 결합도를 낮춘다.

### 2. Eureka + Gateway + JWT + Kafka 구조를 선택한 이유

Eureka는 서비스 인스턴스가 늘거나 줄 때 IP를 하드코딩하지 않고 애플리케이션 이름만으로 상대를 찾게 해준다. API Gateway는 인증 검증을 한 곳에 몰아줘서, 각 도메인 서비스가 JWT를 직접 재검증하지 않고 게이트웨이가 넘겨주는 `X-User-Id` 헤더만 믿으면 되게 만든다.

Kafka 부분을 이해하는 데는 시간이 좀 걸렸다. 수강신청 API를 호출하면 응답은 바로 오는데(`PENDING`), 마이페이지를 새로고침해도 상태가 한동안 `PENDING`으로 남아있어서 처음엔 버그인 줄 알았다. `docker compose logs -f enrollment-service`로 컨슈머 로그를 찍어보고 나서야, payment-service가 `payment.completed` 이벤트를 발행하고 enrollment-service가 그걸 소비해야 비로소 `ACTIVE`로 바뀐다는 걸 알게 됐다. 응답을 기다리지 않고 상태 전이를 이벤트로 분리한 이유(Saga 패턴)가 그제서야 와닿았다.

### 3. 시스템 구조와 요청 처리 흐름

com.lecture.\<domain\>
│
├── config/      → Security(Resource Server), JPA 등 설정
├── controller/  → REST 엔드포인트 정의
├── dto/         → 요청/응답 데이터 구조
├── entity/      → JPA 엔티티(테이블 매핑)
├── repository/  → Spring Data JPA 인터페이스
├── service/     → 비즈니스 로직 (다른 서비스 호출용 WebClient 클라이언트 포함)
└── kafka/       → Producer/Consumer (enrollment-service, payment-service에만 존재)

클라이언트가 API Gateway로 요청(JWT 첨부) →
Gateway가 JWT 검증 후 X-User-Id 헤더로 변환해 하위 서비스로 전달 →
controller가 해당 엔드포인트 함수 실행 →
dto로 요청 데이터 자동 파싱 및 유효성 검증 →
service에서 비즈니스 로직 수행(필요 시 WebClient로 다른 서비스 호출) →
repository로 DB 조회·저장 →
결과를 다시 dto로 포맷팅하여 응답

이 흐름에서 한 번 헷갈렸던 부분은 `config/` 안의 Security 설정이었다. 게이트웨이를 거치지 않고 서비스 포트로 직접 요청을 날리면 `X-User-Id`가 없어서 401이 떨어지는데, 처음에는 이게 인증 서버 문제인 줄 알고 한참 auth-server 로그만 들여다봤다.
