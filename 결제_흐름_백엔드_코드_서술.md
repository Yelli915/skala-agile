# 결제(Payment) 흐름 백엔드 코드 서술

> 프론트엔드에서는 `정산(배송비 + 지자체 지원금)`으로 표기되지만, 백엔드 코드·DTO·테이블·Kafka 토픽 이름은 모두 `payment` 그대로다. 이 문서는 백엔드 구현만 다룬다.

결제 기능은 두 서비스에 걸쳐 있다.

| 서비스 | 역할 | 관련 패키지 |
|---|---|---|
| **payment-service** (`:8084`) | 결제를 실제로 "처리"하고 `payment.completed` 이벤트를 발행 | `com.lecture.payment` |
| **enrollment-service** (`:8083`) | 결제를 요청하고(동기 REST), 결제 완료 이벤트를 수신해 수강을 활성화 | `com.lecture.enrollment` |

payment-service는 아무것도 소비(consume)하지 않고 발행만 한다. 수강 상태를 실제로 바꾸는 쪽은 enrollment-service의 Kafka 컨슈머다.

---

## 1. payment-service 내부 구조

### 1-1. 엔티티 — `entity/Payment.java`

`payments` 테이블에 매핑되는 JPA 엔티티.

| 필드 | 설명 |
|---|---|
| `id` | PK (auto increment) |
| `userId` | 결제한 사용자 (course/enrollment와 마찬가지로 FK 제약 없이 Long 값만 보관) |
| `courseId` | 결제 대상 강의(프로그램) |
| `amount` | 결제 금액 `BigDecimal(10,2)` |
| `status` | `PENDING → COMPLETED / FAILED / CANCELLED` (enum, 문자열로 저장) |
| `transactionId` | 외부 PG 거래 ID 자리. 실습에서는 `UUID.randomUUID()` 문자열로 대체. `unique` 제약 |
| `createdAt` / `updatedAt` | `AuditingEntityListener`로 자동 기록 (`JpaConfig`의 `@EnableJpaAuditing`) |

상태 전이는 setter가 아니라 도메인 메서드로만 가능하다.

```java
public void complete(String transactionId) {
    this.status = Status.COMPLETED;
    this.transactionId = transactionId;
}
public void fail() {
    this.status = Status.FAILED;
}
```

기본 status는 `@Builder.Default`로 `PENDING`이므로, 빌더로 생성하면 항상 PENDING 상태의 행이 먼저 만들어진다.

### 1-2. DTO — `dto/PaymentDto.java`

정적 중첩 클래스 묶음.

- **`InternalPaymentRequest`** — enrollment-service가 보내는 요청 바디. `{ userId, courseId, amount }`. (검증 애노테이션 없음)
- **`InternalPaymentResult`** — 내부 호출에 대한 동기 응답. `{ paymentId, status }` — status는 `"COMPLETED"` 또는 `"FAILED"` 문자열.
- **`PaymentResponse`** / **`PaymentRequest`** — 단건·목록 조회용 및 외부 클라이언트용(현재 컨트롤러에서 `PaymentRequest`를 받는 엔드포인트는 없음).
- **`ApiResponse<T>`** — `{ success, message, data }` 공통 래퍼. 조회 API에서만 사용.

### 1-3. 리포지토리 — `repository/PaymentRepository.java`

`JpaRepository<Payment, Long>` + `findByUserId`, `findByUserIdAndCourseId`, `findByTransactionId`. 결제 처리 흐름에서 실제로 쓰이는 건 기본 `save` 뿐이고, 나머지는 조회 API용이다.

### 1-4. 컨트롤러 — `controller/PaymentController.java`

`@RequestMapping("/api/payments")`

| 메서드 | 경로 | 용도 |
|---|---|---|
| `POST` | `/internal/request` | **결제 처리 진입점.** enrollment-service가 호출. `InternalPaymentRequest` → `InternalPaymentResult` |
| `GET` | `/{id}` | 결제 단건 조회 |
| `GET` | `/user/{userId}` | 사용자 결제 내역 조회 |

`/internal/request`에는 게이트웨이 라우트가 없다. 서비스-투-서비스 전용이며 `X-User-Id`도 JWT도 받지 않는다 (userId를 바디로 직접 받음).

### 1-5. 서비스 — `service/PaymentService.java`

핵심은 `processInternalPayment(...)` 하나. `@Transactional`(쓰기).

```
1. Payment 저장 (status=PENDING)          ← paymentRepository.save(...)
2. transactionId = UUID.randomUUID()      ← PG 연동 대체, 항상 성공
3. payment.complete(transactionId)         ← status=COMPLETED (영속성 컨텍스트 dirty checking으로 UPDATE)
4. kafkaProducer.publishPaymentCompleted() ← payment.completed 토픽으로 발행 (동기, 최대 10초 블로킹)
5. InternalPaymentResult{ paymentId, status="COMPLETED" } 반환
```

- 2번은 실패할 여지가 없다. PG 연동이 없으므로 이 실습에서 결제는 **항상 성공**한다.
- `try/catch`의 catch 블록은 사실상 4번(Kafka 발행)이 예외를 던질 때만 진입한다. 그 경우 `payment.fail()` 후 `status="FAILED"` 결과를 반환한다.
- 단, catch로 들어가도 예외를 다시 던지지 않으므로 `@Transactional`은 **롤백되지 않는다.** PENDING으로 저장된 행이 FAILED로 UPDATE되어 커밋된다.

### 1-6. Kafka 프로듀서 — `kafka/PaymentKafkaProducer.java`

```java
kafkaTemplate.send(paymentCompletedTopic, String.valueOf(event.getUserId()), event)
             .get(10, TimeUnit.SECONDS);   // ★ 동기 대기
```

- 토픽: `${kafka.topic.payment-completed}` → `payment.completed`
- 메시지 키: `userId` 문자열 (같은 사용자 이벤트의 순서 보장 + 파티션 분배)
- 값: `PaymentCompletedEvent { paymentId, userId, courseId, status }`
- `.get(10, SECONDS)`로 **브로커 ack를 동기적으로 기다린다.** "개발/검증 단계에서 전송 성공 여부를 즉시 확인하기 위해"라는 주석대로다. 10초 안에 ack가 없으면 `RuntimeException`을 던지고, 이게 `PaymentService`의 catch로 전파된다.
- 직렬화: `JsonSerializer` + `spring.json.add.type.headers: false` (아래 3번 참고)

### 1-7. 설정 — `application.yml`, `config/*`

- `spring.kafka.producer.value-serializer: JsonSerializer`, `spring.json.add.type.headers: false`
- `kafka.topic.payment-completed: payment.completed`
- `SecurityConfig` — 현재 **모든 요청 `permitAll()`**. OAuth2 리소스 서버 설정(JWT 검증, `/internal/**`를 `SCOPE_service.read`로 제한)은 주석 처리되어 있다. 즉 지금은 8084 포트에 직접 아무나 결제를 요청할 수 있는 상태(실습 단순화).
- `GlobalExceptionHandler` — 컨트롤러 패키지 한정. `IllegalArgumentException`→400, 나머지→500, 각각 `ApiResponse.error(...)`로 감쌈.

---

## 2. enrollment-service 쪽 결제 관련 코드

### 2-1. 결제 요청 — `service/EnrollmentService.enroll(userId, courseId)`

수강신청 흐름의 4단계 중 마지막이 결제 요청이다.

```java
1. courseServiceClient.existsCourse(courseId)              // 없으면 IllegalArgumentException
2. enrollmentRepository.existsByUserIdAndCourseId(...)      // 중복이면 거부
3. enrollmentWriteService.createPendingEnrollment(...)      // REQUIRES_NEW 트랜잭션에서 PENDING 행 즉시 커밋
4. BigDecimal programPrice = courseServiceClient.getCoursePrice(courseId);
   paymentServiceClient.requestPayment(userId, courseId, programPrice);
// → 여기서 바로 EnrollmentResponse(PENDING) 반환. 결제 결과는 기다리지 않음
```

**최근 변경점 (PR #1 / 커밋 `5c0a9be`)**
전에는 금액이 하드코딩이었다.

```java
// 이전
paymentServiceClient.requestPayment(userId, courseId, BigDecimal.valueOf(99000));
// 현재
BigDecimal programPrice = courseServiceClient.getCoursePrice(courseId);
paymentServiceClient.requestPayment(userId, courseId, programPrice);
```

즉 결제 금액을 course-service의 프로그램 실제 가격(`price` 필드)에서 가져오도록 바꿨다.

### 2-2. 가격 조회 — `service/CourseServiceClient.getCoursePrice(courseId)` (신규)

```java
Map<String,Object> courseInfo = getCourse(courseId);          // GET course-service /api/courses/internal/{id}
Object priceValue = courseInfo.get("price");
if (priceValue == null)            throw new IllegalArgumentException("프로그램 가격 정보가 없습니다: " + courseId);
BigDecimal price = new BigDecimal(priceValue.toString());
if (price.compareTo(BigDecimal.ZERO) <= 0)
                                   throw new IllegalArgumentException("프로그램 가격은 0보다 커야 합니다: " + courseId);
return price;
```

- 내부적으로 기존 `getCourse()`(래퍼 응답 `data` / 평문 객체 둘 다 처리)를 재사용한다.
- 가격이 없거나 0 이하이면 `IllegalArgumentException` → enroll 전체가 중단되고 400 응답. 단 이 시점엔 이미 3번에서 PENDING enrollment가 커밋된 뒤라 **PENDING 행은 남는다.**

### 2-3. 결제 요청 클라이언트 — `service/PaymentServiceClient.requestPayment(...)`

```java
WebClient → POST http://payment-service:8084/api/payments/internal/request
   body: { userId, courseId, amount }
   → PaymentResult { paymentId, status }  (.block() 동기 호출)
```

- URL이 Eureka `lb://`가 아니라 **컨테이너 DNS 하드코딩**이다 (`course-service` 호출은 `http://course-service/...`로 포트 없이 lb 스타일인데, payment만 `:8084`까지 박혀 있음 — 일관성 없음).
- 실패 시 `RuntimeException("Payment Service 연결 실패")` → enroll 중단, 하지만 여기도 PENDING enrollment는 이미 커밋됨.
- 반환된 `PaymentResult`는 로그만 찍고 **사용하지 않는다.** 실제 상태 전이는 아래 Kafka 이벤트가 담당한다.

### 2-4. PENDING 행 분리 커밋 — `service/EnrollmentWriteService.createPendingEnrollment(...)`

```java
@Transactional(propagation = Propagation.REQUIRES_NEW)
public Enrollment createPendingEnrollment(Long userId, Long courseId) { ... save ... }
```

`REQUIRES_NEW`로 새 트랜잭션에서 PENDING enrollment를 저장·커밋한다. 이후 결제 요청(2-1의 4번)이 실패해 바깥 로직에서 예외가 나도 이 행은 롤백되지 않는다 — "신청 기록은 남기고, 활성화만 나중에"라는 설계.

### 2-5. 결제 완료 수신 — `kafka/EnrollmentKafkaConsumer.handlePaymentCompleted(...)`

```java
@KafkaListener(topics = "${kafka.topic.payment-completed}",       // payment.completed
               groupId = "${spring.kafka.consumer.group-id}",      // enrollment-service
               containerFactory = "kafkaListenerContainerFactory")
public void handlePaymentCompleted(Map<String, Object> event) {
    Long userId   = ((Number) event.get("userId")).longValue();
    Long courseId = ((Number) event.get("courseId")).longValue();
    enrollmentService.activateEnrollment(userId, courseId);
}
```

- payment-service가 타입 헤더 없이 JSON을 보내므로, DTO로 역직렬화하지 않고 **`Map<String,Object>`로 받아** 필드를 꺼낸다 (`spring.json.value.default.type: java.util.HashMap`).
- `userId`/`courseId`가 없으면 `IllegalArgumentException`.
- **모든 예외를 try/catch로 삼키고 로그만 남긴다.** 즉 활성화가 실패해도 재시도나 DLQ 없이 그냥 흘러간다 (해당 enrollment는 PENDING에 머묾).
- 컨슈머 group-id가 하나(`enrollment-service`)이므로 인스턴스를 여러 개 띄우면 파티션(3개) 단위로 분산 소비된다.

### 2-6. 수강 활성화 — `service/EnrollmentService.activateEnrollment(userId, courseId)`

```java
@Transactional
public void activateEnrollment(Long userId, Long courseId) {
    Enrollment e = enrollmentRepository.findByUserIdAndCourseId(userId, courseId)
                     .orElseThrow(...);           // PENDING 행을 찾음
    e.activate();                                  // status PENDING → ACTIVE
    courseServiceClient.increaseEnrollmentCount(courseId);   // 동기 REST, 실패해도 로그만
    kafkaProducer.publishEnrollmentCompleted(               // enrollment.completed 발행 → recommend-service
        EnrollmentCompletedEvent{ enrollmentId, userId, courseId });
}
```

여기서 상태가 비로소 `ACTIVE`가 되고, 다음 이벤트(`enrollment.completed`)가 나간다.

### 2-7. 설정 — `application.yml`, `config/KafkaConfig.java`

- 컨슈머: `ErrorHandlingDeserializer`로 감싼 `JsonDeserializer`, `spring.json.use.type.headers: false`, `spring.json.value.default.type: java.util.HashMap`, `spring.json.trusted.packages: "*"`, `auto-offset-reset: earliest`
- `KafkaConfig`가 `payment.completed`, `enrollment.completed` 토픽을 각각 파티션 3 / 복제 1로 `NewTopic` 빈 등록 (앱 기동 시 자동 생성)
- `service.payment-service.url: http://payment-service:8084` — 다만 `PaymentServiceClient`는 이 프로퍼티를 안 읽고 URL을 코드에 직접 박아둠

---

## 3. 왜 `Map<String,Object>`로 받나 (직렬화 계약)

payment-service 프로듀서: `JsonSerializer` + `spring.json.add.type.headers: false`
→ 메시지에 `__TypeId__` 헤더가 안 붙는다.

enrollment-service 컨슈머: 타입 헤더가 없으니 어떤 클래스로 역직렬화할지 알 수 없다
→ `spring.json.value.default.type: java.util.HashMap`으로 지정 → 전부 Map으로 들어옴
→ 그래서 컨슈머가 `((Number) event.get("userId")).longValue()`처럼 직접 캐스팅한다.

부작용: 이벤트 필드명(`userId`, `courseId`)이 한쪽에서 바뀌어도 **컴파일 에러가 안 난다.** 런타임에 `null` → `IllegalArgumentException`으로만 드러난다. payment-service의 `KafkaEvent.PaymentCompletedEvent`와 enrollment-service의 `KafkaEvent.PaymentCompletedEvent`는 이름만 같은 별개 클래스이며, 실제로는 아무도 후자로 역직렬화하지 않는다.

---

## 4. 전체 시퀀스 (수강신청 → 결제 → 활성화)

```
[브라우저] POST /api/enrollments { courseId }  (Bearer 토큰)
   → api-gateway: JWT 검증 후 X-User-Id 헤더 추가
   → EnrollmentController.enroll(userId, courseId)
   → EnrollmentService.enroll():
        존재확인 → 중복확인
        → EnrollmentWriteService.createPendingEnrollment()   [REQUIRES_NEW, 즉시 커밋: status=PENDING]
        → CourseServiceClient.getCoursePrice(courseId)        [동기 REST → course-service]
        → PaymentServiceClient.requestPayment(userId, courseId, price)  [동기 REST → payment-service]
              │
              ▼  payment-service PaymentController.processInternalPayment()
              PaymentService.processInternalPayment():
                 save Payment(PENDING)
                 → transactionId = UUID (항상 성공)
                 → payment.complete() → status=COMPLETED
                 → PaymentKafkaProducer.publishPaymentCompleted()
                       → Kafka 토픽 payment.completed 로 발행 (동기, 최대 10초 대기)
              ← { paymentId, status: COMPLETED } 동기 반환 (enrollment-service는 로그만 찍고 버림)
   ← 201 Created + EnrollmentResponse(status=PENDING)   ← 이 시점 enrollment 아직 PENDING

──────── (여기서부터 비동기) ────────

[Kafka] payment.completed  (consumer group: enrollment-service)
   → EnrollmentKafkaConsumer.handlePaymentCompleted(Map event)
   → EnrollmentService.activateEnrollment(userId, courseId):
        enrollment.activate() → status=ACTIVE
        → CourseServiceClient.increaseEnrollmentCount(courseId)  [동기 REST → course-service]
        → EnrollmentKafkaProducer.publishEnrollmentCompleted()
              → Kafka 토픽 enrollment.completed 로 발행

[Kafka] enrollment.completed
   → recommend-service EnrollmentCompletedConsumer (백그라운드 스레드)
   → 현재는 enrollmentId/userId/courseId 로깅만 (추천 캐시 재계산 확장 지점)
```

---

## 5. 이 구현의 특징과 한계 (실습용이라 의도된 것 포함)

| 항목 | 내용 |
|---|---|
| PG 연동 없음 | `UUID` 발급으로 대체, 결제는 항상 성공. 결제 거절·부분취소·환불 시나리오 없음 |
| 보상 트랜잭션 없음 | `getCoursePrice`·`requestPayment` 실패 시 이미 커밋된 PENDING enrollment가 그대로 남음. 정리 로직 없음 |
| 컨슈머가 예외를 삼킴 | `activateEnrollment` 실패해도 재시도·DLQ 없음 → enrollment가 영원히 PENDING일 수 있음 |
| 프로듀서 동기 블로킹 | payment는 `.get(10s)`로 브로커 ack 대기 (검증 편의). enrollment는 비동기 `whenComplete` — 일관성 없음 |
| 멱등성 없음 | 같은 `payment.completed`가 재전달되면 `activate()`가 다시 호출됨 (`increaseEnrollmentCount`도 중복 가능) |
| 이벤트 타입 계약 느슨 | 타입 헤더 미포함 → `Map` 파싱 → 필드명 변경이 컴파일 타임에 안 걸림 |
| 보안 열림 | payment-service `SecurityConfig`가 `permitAll()`, `/internal/**` 요청에 인증 없음 |
| 금액 신뢰 | 결제 금액을 enrollment-service가 course-service에서 조회해 넘김. payment-service는 받은 `amount`를 검증 없이 저장 |
| DB 분리 안 됨 | `payments` 테이블도 공유 `lecture_db`에 있음 (`ddl-auto: update`로 자동 생성) |
