## 아키텍처

서비스 부팅 순서가 중요하며, 이는 `docker-compose.yml`의 `depends_on` 설정을 통해 구성되어 있습니다:

```
MariaDB + Kafka (인프라)
  → eureka-server (서비스 디스커버리, :8761)
    → auth-server (OAuth2/JWT 발급자, :9000 — 빌드 완료된 이미지 제공, 본 저장소에 소스 코드 없음)
      → api-gateway (:8080 — 빌드 완료된 이미지 제공, 본 저장소에 소스 코드 없음) + 4개 도메인 서비스
        → recommend-service (:8085)

```

* **eureka-server** — Netflix Eureka 레지스트리. 모든 Spring 기반 서비스가 이곳에 등록되며 애플리케이션 이름을 통해 서로를 탐색합니다.
* **auth-server** 및 **api-gateway** — 본 저장소에 소스 코드가 포함되어 있지 않습니다. `infra-images.tar`(`docker load -i infra-images.tar`)에서 로드되는 사전 빌드된 이미지 형태로 제공되며, 태그는 각각 `msa-lecture/auth-server:1.0`, `msa-lecture/api-gateway:1.0`입니다. Auth 서버는 JWT를 발급하고 모든 리소스 서버 서비스가 검증에 사용하는 JWKS 엔드포인트(`/oauth2/jwks`)를 노출합니다. 게이트웨이는 인증된 요청을 다운스트림으로 전달할 때 `X-User-Id` 헤더를 추가하며(예: `CourseController.createCourse`), 컨트롤러는 JWT를 직접 재검증하는 대신 이 헤더를 읽어 사용합니다.
* **user-service** (`:8081`), **course-service** (`:8082`), **enrollment-service** (`:8083`), **payment-service** (`:8084`) — Spring Boot 도메인 서비스로, 각 서비스는 `com.lecture.<domain>` 하위에 `config/ controller/ dto/ entity/ repository/ service/` 패키지 구조를 갖습니다. 각 서비스는 Eureka 클라이언트이자 OAuth2 리소스 서버(auth-server의 JWKS를 통해 JWT 검증)이며, 공유 `lecture_db` MariaDB 인스턴스(컨테이너명 `lecturedb`)와 연동되는 JPA 애플리케이션입니다.
* **enrollment-service**는 수강 신청 흐름의 오케스트레이터 역할을 합니다. `WebClient`(`CourseServiceClient`, `PaymentServiceClient`)를 통해 `course-service` 및 `payment-service`를 호출하며, `payment.completed` 및 `enrollment.completed` 토픽에서 Kafka 이벤트(`EnrollmentKafkaProducer`/`Consumer`)를 발행 및 소비합니다.
* **payment-service**는 결제 처리 후 `payment.completed` Kafka 이벤트를 발행하며, 별도로 이벤트를 소비하지는 않습니다.
* **recommend-service** — 규칙 기반 강의 추천을 구현한 Python/FastAPI 애플리케이션(`recommend-service/main.py`, `app/` 패키지 구조)입니다. 시작 시 `py_eureka_client`를 통해 Eureka에 등록하고, 새 수강 신청에 반응하기 위해 `enrollment.completed` Kafka 토픽(`app/kafka/consumer.py`)을 소비하며, `app/client/*_client.py`를 통해 HTTP로 `course-service`/`enrollment-service`를 호출합니다. 설정 우선순위는 `app/config/settings.py`에 명시되어 있습니다: `docker-compose.yml` 환경 변수(런타임) > `.env` (로컬 개발) > `settings.py`의 하드코딩된 기본값.
* **vue-frontend** — Vue 3 + Vite 기반 SPA입니다(로컬 개발 시 `npm run dev`로 `:3000` 구동, Docker 환경에서는 Nginx로 서빙). `src/api/*`는 백엔드 서비스 호출을 래핑하고, `src/store/*`는 Pinia 상태(`auth`, `course`)를 관리하며, `src/views/*`는 라우팅 대상 페이지(랜딩, 로그인/콜백, 강의 목록/상세/등록, 수강 신청, 마이페이지)입니다.
* **init-db/01_init.sql** — MariaDB 컨테이너 최초 실행 시 자동으로 실행됩니다(`/docker-entrypoint-initdb.d`에 마운트됨).

서비스 간 호출 시 Eureka의 `lb://` 클라이언트 측 로드 밸런싱 대신 컨테이너 DNS 이름(예: `http://course-service:8082`, `http://kafka:9092`)을 직접 사용합니다. 특정 호출에 Eureka 기반 디스커버리가 사용된다고 가정하기 전에, 각 서비스의 `application.yml` 내 `service:` / `*_url` 환경 변수 설정을 확인해야 합니다.

## 주요 요청 흐름

여러 폴더 및 서비스에 걸쳐 있는 주요 흐름입니다. 한쪽의 변경(DTO 필드, Kafka 토픽명, 내부 엔드포인트 경로 등)이 다른 쪽과 동기화되어야 하므로 특정 부분을 수정하기 전에 반드시 확인해야 합니다.

### 1. 로그인 (OAuth2 Authorization Code 플로우)

```
vue-frontend (LoginView)
  → auth.store.redirectToLogin(): 전체 페이지 리다이렉트 실행
     auth-server GET /oauth2/authorize?response_type=code&client_id=...&redirect_uri=...
  → auth-server의 로그인 페이지에서 사용자 인증 진행
  → auth-server가 브라우저를 vue-frontend /callback?code=... 로 리다이렉트
vue-frontend (CallbackView)
  → auth.store.handleCallback(code)
  → api/auth.js exchangeCode(): POST auth-server /oauth2/token
     (Basic 인증: base64(client_id:client_secret), grant_type=authorization_code)
  → auth-server가 { access_token } 반환
  → store.setToken() → sessionStorage에 저장; axios 인스턴스(api/index.js)가
     이후의 모든 요청에 `Authorization: Bearer <token>` 헤더를 첨부
  → auth.store.fetchUser(): GET api-gateway /api/users/me (Bearer 토큰 포함)
     → api-gateway가 auth-server의 JWKS를 통해 JWT를 검증하고,
       subject를 추출한 뒤 `X-User-Id` 헤더를 추가하여 user-service로 요청 전달
     → user-service의 UserController.getMe()가 해당 헤더를 읽음 (JWT를 직접 재검증하지 않음)
     → 사용자 프로필 반환 (id, 역할: STUDENT/INSTRUCTOR 등) → Pinia에 저장
이후 router/index.js의 내비게이션 가드가 이 스토어 상태를 기반으로 `requiresAuth` / `instructorOnly` 라우트를 제어합니다.

```

### 2. 강의 탐색 및 등록

```
CourseListView / CourseDetailView → GET /api/courses, GET /api/courses/{id}
CourseCreateView (강사 전용) → POST /api/courses (Bearer 토큰 포함)
  → 게이트웨이가 X-User-Id를 포함하여 전달 → CourseController.createCourse()가
    해당 헤더 값을 instructorId로 사용 (별도의 "강사 식별" 조회 과정 없음)

```

`course-service`는 다른 서비스들이 의존하는 **내부 전용(Internal-only)** 엔드포인트(게이트웨이 라우팅 없음, 서비스 간 직접 호출, `/internal/` 접두사 사용)도 제공합니다:

* `GET /api/courses/internal/exists/{id}` — enrollment-service에서 수강 신청을 수락하기 전에 호출
* `GET /api/courses/internal/{id}` — enrollment-service에서 "내 수강 목록"에 강의 상세 정보를 채우기 위해 호출
* `POST /api/courses/internal/{id}/enrollment-count` — 수강 신청이 활성화되면 강의의 수강생 수를 증가시키기 위해 호출
* `GET /api/courses/internal/recommend?category=&excludeIds=` — recommend-service에서 호출

### 3. 수강 신청 → 결제 사가(Saga) (핵심 분산 트랜잭션 예제)

Kafka 연동이 구축된 핵심 목적을 보여주는 흐름입니다. enrollment-service는 결제가 완전히 완료될 때까지 **기다리지 않고** 즉시 응답을 반환하며, 결과 상태 전이(PENDING → ACTIVE)는 기존 HTTP 요청 내부가 아닌 Kafka 이벤트를 통해 비동기적으로 발생합니다.

```
EnrollmentView → POST /api/enrollments { courseId } (Bearer 토큰 포함)
  → 게이트웨이가 X-User-Id 추가 → EnrollmentController.enroll()
  → EnrollmentService.enroll(userId, courseId):
      1. CourseServiceClient.existsCourse(courseId)        [동기 REST 호출 → course-service]
         → 강의가 존재하지 않으면 404/invalid 반환
      2. enrollmentRepository.existsByUserIdAndCourseId()  [중복 수강 신청 거부]
      3. EnrollmentWriteService.createPendingEnrollment()  [REQUIRES_NEW 트랜잭션 —
         이후 작업의 성공 여부와 무관하게 Enrollment 엔티티를 status=PENDING으로 즉시 커밋하여,
         이후 장애 발생 시에도 롤백되지 않도록 함]
      4. PaymentServiceClient.requestPayment(userId, courseId, amount=99000)
         [동기 REST 호출 → payment-service POST /api/payments/internal/request]
  ← 이 시점에 브라우저에 201 Created 응답 반환 — 수강 신청은 아직 PENDING 상태이며,
    호출자는 결제 최종 결과를 아직 알지 못함.

payment-service PaymentController.processInternalPayment():
  → PaymentService.processInternalPayment():
      1. Payment 저장 (PENDING 상태)
      2. 결제 "처리" — 실제 PG사 연동 없이 항상 성공하며, 랜덤 UUID를 트랜잭션 ID로 발급
      3. payment.complete() → status COMPLETED로 변경
      4. PaymentKafkaProducer.publishPaymentCompleted() → Kafka 토픽 `payment.completed`로 발행
         (동기 방식으로 전송되며, 브로커 ack까지 최대 10초 대기)
  ← enrollment-service의 WebClient 호출에 { paymentId, status: COMPLETED }를 동기 반환
    (참고용 데이터이며, 실제 상태 변경은 이 반환값이 아닌 아래 Kafka 이벤트를 통해 진행됨)

enrollment-service EnrollmentKafkaConsumer (토픽 `payment.completed`, 컨슈머 그룹 `enrollment-service`):
  → 원시 이벤트 파싱 (payment-service의 JsonSerializer가 타입 헤더를 포함하지 않으므로 Map<String,Object> 형태로 수신)
  → EnrollmentService.activateEnrollment(userId, courseId):
      1. Enrollment 엔티티 로드, enrollment.activate() → status ACTIVE로 변경
      2. CourseServiceClient.increaseEnrollmentCount(courseId)  [동기 REST 호출 → course-service]
      3. EnrollmentKafkaProducer.publishEnrollmentCompleted() → Kafka 토픽 `enrollment.completed`로 발행

recommend-service EnrollmentCompletedConsumer (app/kafka/consumer.py, 백그라운드 스레드, 토픽 `enrollment.completed`):
  → enrollmentId/userId/courseId 로깅 (캐시 무효화 및 재계산을 위한 확장 지점으로 명시되어 있으며,
    현재는 로깅 외에 별도 동작 없는 no-op 상태)

```

"내 수강 목록" 조회(`GET /api/enrollments/my`)는 별도의 읽기 경로를 따릅니다: `EnrollmentService.getEnrollmentsByUser()`가 사용자의 `Enrollment` 엔티티들을 조회한 후, 각 항목마다 `CourseServiceClient.getCourse()`를 동기 호출하여 강의 세부 정보(제목, 가격, 썸네일, 강사 등)를 결합한 뒤 프론트엔드에 반환합니다. 따라서 수강 신청 데이터 자체는 enrollment-service의 테이블에 존재하더라도, 해당 페이지를 온전히 렌더링하려면 course-service에 접근할 수 있어야 합니다.

### 4. 추천 시스템

```
MyPageView (학생 뷰) → GET /api/recommend/{userId} (Bearer 토큰 포함,
  recommend-service 자체 app/config/security.py의 JWT 검증을 거침 — X-User-Id 헤더를 신뢰하지 않고 동일한 JWKS를 기반으로 검증)
  → recommend_router.get_recommendations()
  → RecommendService.get_recommendations(user_id):
      1. EnrollmentServiceClient.get_enrollment_history(user_id)
         [GET enrollment-service /api/enrollments/internal/history/{userId}
          → { activeCourseIds: [...] }; 추천은 핵심 기능이 아니므로 실패 시 "수강 이력 없음"으로 처리]
      2. 수강 이력 없음 → _recommend_for_new_user(): CourseServiceClient.get_all_courses()
         호출 후 enrollmentCount 내림차순 정렬하여 상위 5개 선택
      3. 수강 이력 있음 → CourseServiceClient.get_all_courses()를 통해 수강 중인 각 강의의 카테고리를 확인하고,
         가장 빈도가 높은(Counter.most_common) 카테고리를 선택한 뒤, CourseServiceClient.get_recommend_courses(category, exclude_ids=activeCourseIds)
         [GET course-service /api/courses/internal/recommend] 호출 → 상위 5개 선택

```

이는 순수 읽기 시점 연산(추천 상태를 영속 저장하지 않음)입니다. 만약 이 프로젝트가 단순 강의용 예제를 넘어 확장된다면, 3번 흐름의 Kafka 컨슈머에 캐싱 또는 사전 연산 단계가 추가될 수 있습니다.

## 주요 명령어

### 백엔드 (eureka-server, user-service, course-service, enrollment-service, payment-service 각각 독립된 Gradle 래퍼 보유)

각 서비스 디렉토리 내부에서 실행합니다 (예: `cd course-service`):

```bash
./gradlew build              # 컴파일 + 테스트 + 패키징
./gradlew test               # 전체 테스트 실행
./gradlew test --tests "com.lecture.course.CourseServiceApplicationTests"   # 특정 테스트 클래스 실행
./gradlew bootRun             # 로컬에서 서비스 실행 (localhost에 MariaDB/Eureka/auth-server가 동작 중이어야 함)

```

### Docker Compose를 통한 전체 스택 실행 (저장소 루트 디렉토리 기준)

```bash
docker load -i infra-images.tar        # 사전 빌드된 auth-server / api-gateway 이미지 로드 (최초 1회 / 저장소 pull 후 실행)
docker compose build --no-cache && docker compose up -d   # 로컬 서비스 빌드 + 전체 컨테이너 시작
docker compose logs -f [서비스명]        # 예: eureka-server, auth-server, api-gateway, user-service, course-service, enrollment-service, payment-service, recommend-service, mariadb, kafka
docker compose down                     # 컨테이너 중지 및 제거

```

모든 서비스가 정상 등록되었는지 확인하려면 `http://localhost:8761/`에서 Eureka 대시보드를 확인하십시오.

### 프론트엔드

```bash
cd vue-frontend
npm install
npm run dev       # http://localhost:3000 에서 개발 서버 구동
npm run build      # 프로덕션 빌드

```

### recommend-service (Python/FastAPI)

```bash
cd recommend-service
pip install -r requirements.txt
python main.py     # 또는: uvicorn main:app --reload --port 8085

```

## 서비스 수정 시 주의사항

* MariaDB 접속 정보(`manager`/`SqlDba-1`, DB명 `lecture_db`) 및 JWKS/발급자(issuer) URI는 각 서비스의 `application.yml`에 중복 설정되어 있습니다. 하나를 변경할 때(예: 자격 증명 변경 또는 auth-server 주소 이동) `user-service`, `course-service`, `enrollment-service`, `payment-service` 전체를 함께 업데이트해야 합니다.
* `application.yml`의 `datasource.url`/`eureka.client.service-url`은 로컬 `bootRun`을 위해 `localhost`를 가리키고 있지만, Docker 환경에서는 `docker-compose.yml` 환경 변수(`SPRING_PROFILES_ACTIVE=docker`, `SPRING_DATASOURCE_URL` 등)에 의해 컨테이너 이름(`lecturedb`, `eureka-server`, `auth-server`)으로 오버라이드됩니다. YAML 기본값이 Docker 상에서 그대로 사용된다고 가정하지 마십시오.
* 모든 도메인 서비스에 `hibernate.ddl-auto: update`가 설정되어 있으므로, 엔티티를 수정하면 다음 부팅 시 공유 스키마가 자동으로 마이그레이션됩니다. 본 저장소에는 별도의 DB 마이그레이션 도구(Flyway/Liquibase)가 포함되어 있지 않습니다.
* Kafka 토픽 이름(`payment.completed`, `enrollment.completed`)은 각 서비스의 `application.yml` 내 `kafka.topic` 블록에서 서비스별로 설정됩니다. `payment-service`, `enrollment-service`, `recommend-service` 간에 프로듀서 및 컨슈머 토픽 문자열이 항상 일치하도록 유지해야 합니다.