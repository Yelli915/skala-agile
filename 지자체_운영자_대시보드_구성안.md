# 지자체 운영자 대시보드 구성안

## 배경

기존 "강사 대시보드" 구조를 그대로 재사용한다는 전제 하에, 지자체 운영자(담당자)용 대시보드를 새로 만들지 않고 프론트엔드 라벨만 교체해서 구현하는 안이다. [프론트_백엔드_용어_매핑표.md](./프론트_백엔드_용어_매핑표.md), `CLAUDE.md`의 "Planned domain migration" 절과 동일한 원칙을 따른다:

> **백엔드(user/course/enrollment/payment/recommend-service)의 이름·API·DB·Kafka 토픽은 전혀 바꾸지 않는다. `vue-frontend`만 수정한다.**

DoD(문서 기준) 2·3·4·5번 — 프로그램 등록, 목록/상세 표시, 참여 건수 확인, 지원대상·배송비 기준 표시 — 을 만족시키는 것이 목표.

## 대시보드 구성요소 및 기존 화면 매핑

| 구성요소 | 기존 화면(강사용) | FE 수정 범위 | 대응 DoD |
|---|---|---|---|
| 요약 통계 카드 (등록 프로그램 수 / 총 신청 건수 / 진행중 프로그램 수) | 강사 대시보드 상단 요약(강의 수/수강생 수) | 카드 라벨만 교체 | DoD 4 |
| 내 프로그램 목록 | '내 강의' 목록 컴포넌트 | 필드 라벨 매핑(강의명→프로그램명 등) | DoD 3 |
| 프로그램 등록 버튼/폼 | 강의 등록 폼 | 입력 필드 라벨 및 placeholder 문구 교체 | DoD 2 |
| 프로그램 상세/관리 화면 | 강의 상세(강사 편집 모드) | 지원 대상, 배송비 기준 항목이 상단에 노출되도록 배치 우선순위 조정 | DoD 5 |
| 참여 현황(신청자 목록/건수) | 수강생 목록 | 상태값 라벨 매핑(신청/승인/처리중 등) | DoD 4 |
| 카테고리별 프로그램 필터 | 강의 카테고리 필터 | 카테고리명을 '전통시장 당일배송', '정기 묶음배송', '냉장 공동배송' 등으로 교체 | DoD 3 |

## 구성 시 유의사항

- **API/데이터는 그대로, 라벨만 매핑**: 요약 통계는 기존 `courseCount`, `enrollmentCount` 같은 필드를 그대로 쓰고, 화면 텍스트만 '프로그램 수', '신청 건수'로 바꾼다.
- **프로그램 등록 폼 필드 우선순위**: DoD 5번(지원 대상·배송비 기준 화면 표시)을 만족시키려면, 기존 강의 등록 폼의 필드 중 '커리큘럼'이나 '수강료' 입력란을 각각 '지원 대상 설명', '배송비/지원 기준'으로 라벨링해 운영자가 그 값을 직접 입력하고, 이 값이 상세 화면 상단에 그대로 노출되게 배치한다.
- **참여 건수는 목록/상세 둘 다에 노출**: 대시보드 요약 카드뿐 아니라 개별 프로그램 상세 화면에도 "현재 N건 신청"처럼 카운트를 보여주면 DoD 4번을 명확히 충족한다.
- **역할 구분 유지**: 로그인 시 role(강사=운영자)에 따라 기존에 분기되던 라우팅/메뉴 구조는 그대로 두고, 메뉴명만 '내 강의 관리→공동물류 프로그램 관리' 식으로 바꾸면 충돌 없이 재사용 가능하다.
- **발표용 스냅샷 고려**: Sprint Board의 "촬영할 스냅샷" 항목 중 Eureka 등록 화면 외에, 대시보드 요약 카드 + 프로그램 등록 폼 화면도 스냅샷 후보로 추가해두면 발표 3번 순서("지자체 운영자의 프로그램 등록")와 바로 연결된다.

## 네이밍 관련 평가

- 구조 재사용 방향 자체는 `CLAUDE.md`의 최우선 제약(백엔드 불변, `vue-frontend`만 수정)과 기존 `INSTRUCTOR → 지자체 담당자` 매핑에 정확히 부합하므로 리스크가 낮다.
- 다만 화면 이름을 **"관리자(admin)"**로 붙이면, 실제 권한 범위(본인이 등록한 프로그램만 조회·관리하는 `INSTRUCTOR` 권한)보다 넓은 인상(다른 담당자의 프로그램까지 총괄하는 시스템 전체 관리자)을 줄 수 있다.
- 따라서 라벨은 "관리자"보다 **"담당자/운영자"**로 통일하는 것을 권장한다 — 문서에서 이미 쓰고 있는 용어이고, 실제 백엔드 스코프(본인 소유 리소스 한정)와 의미가 정확히 맞아떨어진다.

## 참고

- 이 문서에 없는 새 화면 문구·카테고리 라벨이 필요해지면 [프론트_백엔드_용어_매핑표.md](./프론트_백엔드_용어_매핑표.md)와 `CLAUDE.md`의 매핑 표도 함께 갱신한다.
