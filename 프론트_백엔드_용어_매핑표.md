# 프론트엔드 ↔ 백엔드 용어 매핑표

## 배경

이 프로젝트는 원래 "MSA 강의 플랫폼(강의/수강신청/결제)" 예제입니다. 화면에는 **지역 공동물류 플랫폼(물류이음)** 도메인으로 보이도록 `vue-frontend`만 수정했고, **백엔드(user/course/enrollment/payment/recommend-service)의 이름·API·DB·Kafka 토픽은 전혀 바뀌지 않았습니다.**

즉 지금 이 저장소는 **하나의 서버 코드 위에 두 개의 이름 체계**가 겹쳐 있는 상태입니다:

- **백엔드 실체(코드/DB/API)** — 여전히 강의 플랫폼 용어 (`Course`, `Enrollment`, `INSTRUCTOR`, `STUDENT` 등)
- **프론트 화면(사용자 눈에 보이는 텍스트)** — 공동물류 플랫폼 용어 (`프로그램`, `참여신청`, `지자체 담당자`, `소상공인` 등)

백엔드 API를 직접 호출하거나(Postman 등), Swagger/DB를 볼 때는 옛 이름이 그대로 보이는 게 정상입니다. 이 문서는 "화면에 보이는 이름 A는 실제로 백엔드의 무엇 B인가"를 찾기 위한 대조표입니다.

## 브랜드

| 프론트 표시 | 백엔드/코드상 실체 |
|---|---|
| 물류이음 | (백엔드에 해당 개념 없음 — 순수 프론트 UI 문자열. `index.html` `<title>`, `AppHeader.vue`, `LandingView.vue`, `LoginView.vue`) |

## 역할(Role)

| 프론트 표시 | 백엔드 실제 값 | 비고 |
|---|---|---|
| 지자체 담당자 | `User.role = "INSTRUCTOR"` | DB 컬럼/JWT 클레임/API 요청·응답 모두 `INSTRUCTOR` 그대로. 강의를 개설하던 역할 = 공동물류 프로그램을 개설하는 역할로 재해석 |
| 소상공인 | `User.role = "STUDENT"` | DB 컬럼/API 모두 `STUDENT` 그대로. 강의를 수강하던 역할 = 프로그램에 참여하는 역할로 재해석 |

회원가입 폼(`LoginView.vue`)의 `<select>` 값도 `value="STUDENT"` / `value="INSTRUCTOR"`로 그대로 전송되고, 화면 라벨만 "소상공인"/"지자체 담당자"로 보입니다.

## 핵심 엔티티

| 프론트 표시 | 백엔드 실체 | 관련 서비스 / 필드 |
|---|---|---|
| (공동물류) 프로그램 | `Course` | `course-service`. 필드명 그대로: `title`, `description`, `category`, `price`, `instructorName`, `enrollmentCount` 등. API 경로도 `/api/courses`, `/api/courses/{id}`, `/api/courses/internal/*` 그대로 |
| 참여신청 / 참여 | `Enrollment` | `enrollment-service`. 상태값도 그대로 `PENDING` → `ACTIVE`. API `/api/enrollments`, `/api/enrollments/my`, `/api/enrollments/internal/history/{userId}` 그대로 |
| 정산 | `Payment` | `payment-service`. API `/api/payments/internal/request` 그대로. Kafka 토픽도 `payment.completed` 그대로 (안 바뀜) |
| 추천 프로그램 | recommend-service 응답 | `GET /api/recommend/{userId}` 그대로. Kafka 소비 토픽도 `enrollment.completed` 그대로 (안 바뀜) |

## 카테고리 라벨 (= 화면상 "배송유형")

백엔드 `Course.Category` enum 값은 그대로 두고, 화면 라벨만 "배송유형"으로 재해석합니다.
매핑 단일 소스는 `vue-frontend/src/store/course.js`의 `CATEGORY_META` 하나이며,
`categoryMeta(value)`가 `{ label, badge, bg, emoji, blurb }`를 돌려줍니다.
모든 화면(`CourseCard.vue`, `CourseListView.vue`, `CourseDetailView.vue`, `EnrollmentView.vue`,
`MyPageView.vue`, `CourseCreateView.vue` 폼 옵션)이 이 함수만 사용합니다 — 화면별 로컬 매핑은 없습니다.

분류 축은 **상품군이 아니라 "공동배송을 묶는 방식(배송유형)"** 입니다. enum 8종을 모두 커버합니다.

| 프론트 표시 (배송유형) | 아이콘 | 백엔드 enum 값 |
|---|---|---|
| 당일 공동배송 | 🚚 | `BACKEND` |
| 정기 묶음배송 | 📦 | `FRONTEND` |
| 냉장·신선 공동배송 | ❄️ | `DEVOPS` |
| 새벽 배송 | 🌙 | `DATA_SCIENCE` |
| 권역 라스트마일 | 📍 | `MOBILE` |
| 반품·회수 물류 | 🔄 | `SECURITY` |
| 대형화물 공동배차 | 🚛 | `DATABASE` |
| 기타 | 📋 | `OTHER` |

> 카드 썸네일은 별도 이미지 자산 없이 **배송유형 컬러 배경 + 이모지**로 렌더링합니다
> (`src/assets/images/courses/*.png` 개발 강의 이미지 6종은 제거됨).
> 국문 폰트는 `pretendard`(variable, dynamic-subset)로 통일 — `--font-sans` 참고.

## 화면 문구 대조

| 프론트 표시 | 원래(백엔드 관점) 의미 |
|---|---|
| 공동물류 프로그램 / 프로그램 등록 / 프로그램명 | 강의 목록 / 강의 등록 / 강의명 |
| 지원 대상·배송 권역·지원 내용 (등록 폼) | 강의 설명 (`description` 필드 그대로) |
| 내 참여 현황 / 참여 신청 / 참여 확정 | 내 수강 목록 / 수강 신청 / 수강 중 |
| 참여 소상공인 (N명) / 현재 N건 참여 신청 | 수강생 (N명) — `enrollmentCount` 필드 값 그대로 표시 |
| 정산하고 참여하기 / 정산 처리 중 | 결제하고 수강하기 / 결제 처리 중 |
| 운영 주체 : (이름) | 강사 : (이름) — `instructorName` 필드 값 그대로 표시 |
| 참여 진행 단계 스텝퍼 (신청 접수 → 정산 완료 → 참여 확정) | `Enrollment.status` `PENDING`(1단계 완료) → `ACTIVE`(전체 완료) 시각화 |

## 이 매핑이 실제로 구현된 위치

- `vue-frontend/src/store/course.js` — `CATEGORY_META`(단일 소스), `categories`, `categoryMeta()`
- `vue-frontend/src/components/AppHeader.vue`, `AppSidebar.vue`(공통 사이드바), `CourseCard.vue`
- `vue-frontend/src/views/*.vue` — 전체 화면 텍스트
- `vue-frontend/src/assets/styles/global.css` — `--font-sans`(Pretendard), 배송유형 배지/보조 그린 토큰
- `CLAUDE.md`의 "Planned domain migration" 절 — 이 매핑의 원본 결정 기록
- `지자체_운영자_대시보드_구성안.md` — 담당자 화면 구성 근거

## 주의사항 (새로 작업할 때)

- **백엔드 코드/DB/API/Kafka 토픽 이름은 절대 바꾸지 않는다.** 새 화면을 추가할 때도 API 요청·응답 필드명은 항상 위 표의 "백엔드 실체" 컬럼 이름을 그대로 사용해야 한다 (예: 요청 바디에 `courseId`를 넣어야지 `programId`를 넣으면 안 됨).
- 새로운 화면 문구를 추가할 때 이 표에 없는 새 용어가 필요하면, 이 문서와 `CLAUDE.md`의 매핑 표를 함께 갱신한다.
- Postman/curl로 API를 직접 호출하거나 Swagger, DB, 로그를 볼 때는 이 문서의 "백엔드 실체" 이름으로 찾아야 한다 (화면 이름으로 검색하면 코드에 없다).
